build instructions
------------------

Requirements:
loop devices in /dev that are accessible by a normal user.

./configure
make toolchain
make apps_port
make man
make newhd
make all

then to test in Qemu: make all test
